{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Run dev site",
			"command": "npx tinacms dev -c 'hugo server -D --appendPort false --noBuildLock'",
			"group": "build"
		}
	]
}